# 中国大陆城市交通枢纽中转决策工具 PRD

> 文档状态：V1.0 Draft\
> 产品阶段：MVP / 个人使用优先\
> 目标平台：Web（响应式，优先桌面端与手机浏览器）\
> 目标区域：中国大陆\
> 文档日期：2026-09-14

------------------------------------------------------------------------

## 1. 产品概述

### 1.1 产品背景

在跨城市旅行中，用户经常需要在某个中间城市完成"飞机 → 铁路"或"铁路 →
铁路"等跨枢纽中转。大型城市通常同时拥有多个机场、火车站和高铁站，而现有
OTA、地图及铁路购票产品通常以完整 A→B
行程或单一交通方式为核心，难以快速回答：

-   某个中转城市有哪些可用机场和铁路客运站？
-   从当前机场/车站去哪个铁路站最方便？
-   指定日期、指定到达时间后，哪些车次实际上还能赶上？
-   地图显示的 45
    分钟接驳时间，在考虑下机、行李、进站安检等因素后是否安全？
-   目标城市有多个铁路站时，应该选择哪个？
-   是否存在目标城市附近的替代铁路站或机场，使整体中转更合理？

本产品希望提供一个以"城市交通枢纽关系"为核心的中转决策工具。

### 1.2 一句话定位

**帮助用户快速判断：在一个陌生城市中转时，应该去哪个机场/火车站，以及应该预留多少时间。**

### 1.3 产品原则

1.  **先解决中转决策，不做另一个 OTA。**
2.  **安全可行优先于理论最快。**
3.  **结果可解释。** 推荐必须告诉用户为什么。
4.  **时间优先于价格。** V1 不比较票价。
5.  **真实车次优于抽象可达性。** 指定日期后展示具体铁路车次。
6.  **默认简单，信息可展开。** 首页快速得到结论，详细路线按需查看。
7.  **数据不确定性必须显式呈现。** 不把估算时间包装成绝对保证。

------------------------------------------------------------------------

## 2. 产品目标与非目标

### 2.1 V1 产品目标

V1 聚焦"已确定/正在考虑某个中转城市"的场景，完成：

1.  查询中国大陆城市内主要机场及铁路客运站。
2.  查询当前枢纽到候选枢纽的公共交通和驾车/打车接驳方案。
3.  根据日期查询候选铁路站到目标城市的具体铁路车次。
4.  根据用户预计到达时间、托运行李状态及市内接驳时间，计算安全中转时间。
5.  自动排除明显赶不上的车次。
6.  对候选铁路站进行排序并给出推荐理由。
7.  默认搜索行政意义上属于目标城市的铁路站。
8.  可选展示目标城市附近具有实际替代价值的铁路站和机场。

### 2.2 V1 非目标

V1 明确不包含：

-   火车票/机票价格比较；
-   余票保证或购票；
-   酒店、景点、餐饮推荐；
-   汽车客运站及长途客运班次；
-   国际及港澳台交通；
-   完整 A→B 自动中转城市发现；
-   飞机→飞机完整联程推荐；
-   铁路→飞机完整联程推荐；
-   订单管理；
-   用户社区；
-   对航班/列车准点作任何保证。

以上能力可在后续版本评估。

------------------------------------------------------------------------

## 3. 目标用户与核心场景

### 3.1 目标用户

V1 首先服务产品创建者本人及具有类似需求的自由行用户：

-   经常跨城市旅行；
-   会主动组合飞机、铁路等不同交通方式；
-   愿意为了更好的时间安排使用非常规中转路线；
-   对城市内不同铁路站不熟悉；
-   比单纯最低价格更关心总耗时、中转风险和便利程度。

### 3.2 核心用户故事

**US-01 城市枢纽查询**

> 作为旅行者，我想输入一个中转城市，快速看到这个城市主要有哪些机场和铁路客运站，以便理解城市交通结构。

**US-02 机场→铁路站选择**

> 作为刚乘飞机到达中转城市的旅行者，我想知道哪个铁路站既容易从机场到达，又有去目标城市的合适列车。

**US-03 安全车次筛选**

> 作为旅行者，我想输入航班预计落地时间和是否托运行李，让系统自动排除理论上存在但实际上很难赶上的列车。

**US-04 接驳方式比较**

> 作为旅行者，我希望同时看到公共交通和打车方案，以便在时间紧张时判断打车是否能让我赶上更早的列车。

**US-05 多铁路站目的地**

> 作为旅行者，我输入"苏州"后，希望系统自动考虑属于苏州市的多个铁路客运站，而不是要求我提前知道应该搜索苏州站还是苏州北站。

**US-06 替代枢纽**

> 作为更灵活的旅行者，我希望打开"附近替代枢纽"，发现目标城市周边可能更合适的铁路站或机场。

------------------------------------------------------------------------

## 4. V1 核心流程

### 4.1 标准流程

用户输入：

1.  中转城市；
2.  当前/到达枢纽；
3.  到达日期；
4.  预计到达时间；
5.  下一程目标城市；
6.  是否托运行李；
7.  是否显示附近替代枢纽。

系统执行：

`识别城市 → 加载城市枢纽 → 获取市内接驳 → 获取目标城市铁路站 → 查询指定日期车次 → 计算安全可乘时间 → 过滤车次 → 候选站评分 → 输出推荐`

### 4.2 示例

输入：

-   中转城市：成都
-   到达：成都天府国际机场
-   日期：2026-10-03
-   预计落地：14:20
-   目标城市：乐山
-   托运行李：有
-   附近替代枢纽：关闭

输出示意：

**推荐：成都东站**

-   公共交通：约 65 分钟
-   打车：约 50--70 分钟
-   推荐安全中转时间：约 2 小时 30 分钟
-   建议选择：16:50 后出发的列车
-   当日符合安全条件的直达车次：N 班
-   推荐原因：可乘车次多、中转容错较高、接驳稳定

候选列表：

  候选站        接驳时间   安全可乘车次 最早建议车次   推荐
  ---------- ----------- -------------- -------------- -------
  成都东站     约 65 min              N 16:50 后       ★★★★★
  成都南站     约 55 min              N 16:40 后       ★★★★☆
  成都西站     约 80 min              N ---            ★★☆☆☆

具体数据以实际接口返回为准。

------------------------------------------------------------------------

## 5. 信息架构与页面设计

### 5.1 页面 P0：首页 / 查询页

核心输入区：

-   中转城市 `必填`
-   当前/到达枢纽 `必填`
-   日期 `必填`
-   预计到达时间 `必填`
-   目标城市 `必填`
-   托运行李：
    -   无托运
    -   有托运
    -   不确定
-   接驳偏好：
    -   综合推荐（默认）
    -   公共交通优先
    -   打车优先
-   `显示附近替代枢纽` 开关
-   查询按钮

交互要求：

-   中转城市选择后，当前枢纽自动联想该城市机场/铁路站。
-   目标城市输入城市名即可，不要求用户输入具体铁路站。
-   日期默认今天或最近合理日期，但必须明显展示，避免用户误查日期。
-   到达时间应明确表示"飞机预计落地/列车预计到站时间"。

### 5.2 页面 P1：结果页

结果页从上到下分为：

#### A. 推荐摘要

展示：

-   首选铁路站；
-   推荐接驳方式；
-   推荐安全中转时间；
-   最早建议购买的列车；
-   一句话推荐理由。

#### B. 候选枢纽卡片

每个候选站展示：

-   站名；
-   类型；
-   当前枢纽→该站公共交通时间；
-   打车预计时间；
-   换乘次数；
-   步行距离（数据可用时）；
-   安全可乘车次数；
-   最早安全车次；
-   推荐等级；
-   风险标签。

风险标签示例：

-   `宽裕`
-   `可行`
-   `偏紧`
-   `不建议`
-   `无合适车次`

#### C. 铁路车次

每个候选站可展开：

  车次   出发站   出发   到达站   到达   历时   中转余量   状态
  ------ -------- ------ -------- ------ ------ ---------- ------

状态：

-   安全
-   可赶但偏紧
-   理论不可行

默认隐藏理论不可行车次，可由用户手动展开。

#### D. 市内接驳详情

公共交通方案应尽可能展示：

`步行 → 地铁/公交 → 换乘 → 步行 → 铁路站`

包含：

-   总时间；
-   总步行；
-   换乘次数；
-   线路名称；
-   每段时间；
-   是否存在夜间运营风险。

打车方案展示：

-   预计驾驶时间；
-   距离；
-   时间波动提示。

V1 不需要展示打车价格。

### 5.3 页面 P2：城市枢纽浏览

允许不输入目标城市，仅查看：

> "南京有哪些主要交通枢纽？"

展示：

-   机场；
-   铁路客运站；
-   地图位置；
-   枢纽间典型接驳时间。

可作为产品的独立探索入口。

------------------------------------------------------------------------

## 6. 枢纽数据模型

### 6.1 Hub

``` text
Hub
- id
- canonical_name
- aliases[]
- type: AIRPORT | RAILWAY
- city_name
- city_adcode
- province
- longitude
- latitude
- amap_poi_id
- railway_station_code
- active
- importance_level
- source
- last_verified_at
```

### 6.2 RailwayTrip

``` text
RailwayTrip
- train_no
- service_date
- origin_station
- destination_station
- departure_time
- arrival_time
- duration
- train_type
- stops[]
- data_source
- updated_at
```

支持：

-   G 高速动车组；
-   D 动车组；
-   C 城际；
-   Z/T/K 等普速列车；
-   其他中国国家铁路客运车次。

### 6.3 TransferRoute

``` text
TransferRoute
- origin_hub_id
- destination_hub_id
- mode
- duration_seconds
- distance_meters
- walking_distance
- transfer_count
- segments[]
- calculated_at
- provider
```

------------------------------------------------------------------------

## 7. 数据源方案

### 7.1 城市 POI / 枢纽

V1 推荐使用：

**高德 Web Service POI + 本地维护的枢纽基础表。**

原因：

-   POI API 适合发现和校验机场/车站；
-   纯 POI 搜索可能混入地铁站、货运站、已停用车站；
-   因此系统需要自己的 `Hub Registry` 做标准化。

建议：

首次发现 → 人工/规则校验 → 写入 Hub Registry → 定期校验。

### 7.2 城市内接驳

首选高德 Web Service 路径规划。

需要支持：

-   公交/地铁；
-   驾车；
-   步行分段。

系统只存短期缓存，不应长期把地图预计时间视为静态事实。

### 7.3 铁路时刻表

V1 数据层应做成 Provider Adapter，避免绑定单一来源。

``` text
RailProvider
  ├─ GTFSProvider
  ├─ RailwayQueryProvider
  └─ FutureOfficialProvider
```

MVP 可优先研究/使用公开 GTFS 数据完成原型。

注意：开源 `chinese-railway-gtfs` 提供中国国家铁路车站、线路和时刻表等
GTFS
数据，并称其数据每周更新；但其铁路时刻来源的获取方法并未公开。因此它适合作为个人原型和数据结构验证来源，但在公开发布/商业化前必须重新评估数据授权、稳定性及合规性。

### 7.4 坐标系

必须明确处理：

-   铁路 GTFS / OSM 常见 WGS84；
-   高德使用 GCJ-02 体系。

不得直接混用坐标。

应在 Data Layer 中统一：

``` text
source coordinates
      ↓
Coordinate Normalizer
      ↓
internal canonical coordinates
      ↓
provider-specific conversion
```

------------------------------------------------------------------------

## 8. 安全中转时间算法

### 8.1 核心定义

**Safe Transfer Time（STT）**

不是地图显示的 A→B 时间，而是：

`前序交通释放时间 + 枢纽内部耗时 + 市内接驳 + 下一程进站准备 + 风险缓冲`

### 8.2 飞机 → 铁路

初始模型：

``` text
STT =
下机缓冲
+ 行李缓冲
+ 机场内部步行
+ 市内接驳预计时间
+ 铁路进站缓冲
+ 风险 Buffer
```

参数建议作为配置项，而不是写死。

初始默认值可采用产品经验值进行 MVP 测试，例如：

  参数                无托运   有托运   不确定
  ----------------- -------- -------- --------
  下机/离开机舱       15 min   15 min   15 min
  行李                     0   25 min   20 min
  机场内部移动        15 min   15 min   15 min
  铁路进站            30 min   30 min   30 min
  基础风险 Buffer     20 min   25 min   25 min

以上仅为产品初始参数，不应向用户表述为行业标准。

### 8.3 铁路 → 铁路（跨站）

``` text
STT =
前序列车下车/出站
+ 市内接驳
+ 目标站进站
+ 风险 Buffer
```

### 8.4 动态修正

后续可增加：

-   机场规模；
-   航站楼；
-   国内/国际到达；
-   节假日；
-   高峰时段；
-   公交换乘次数；
-   末班车；
-   历史航班晚点；
-   极端天气；
-   用户熟悉程度。

### 8.5 输出等级

不建议只输出单一"安全/不安全"。

定义：

**宽裕**

`车次出发 - 理论到站 ≥ 推荐时间 + 大 Buffer`

**安全**

满足推荐 STT。

**偏紧**

低于推荐 STT，但高于最短可行时间。

**不可行**

低于最短可行时间。

UI 必须强调：

> 中转时间为规划建议，不代表航班、道路、公共交通或铁路实际运行保证。

------------------------------------------------------------------------

## 9. 候选枢纽推荐算法

### 9.1 Hard Filter

首先排除：

1.  指定日期无目标城市方向列车；
2.  所有列车均早于最短可行时间；
3.  枢纽已停运；
4.  无可行城市内接驳；
5.  数据明显过期或异常。

### 9.2 初始评分

候选站 Score：

``` text
Score =
0.35 × TransferConvenience
+ 0.30 × TrainAvailability
+ 0.25 × SafetyMargin
+ 0.10 × TransferSimplicity
```

其中：

**TransferConvenience**

综合：

-   市内接驳时间；
-   公交/地铁可达性；
-   打车时间。

**TrainAvailability**

综合：

-   安全时间之后车次数量；
-   班次时间分布；
-   是否直达。

**SafetyMargin**

综合：

-   最早可选车次距离 STT 的余量；
-   后续备用车次数量。

**TransferSimplicity**

综合：

-   换乘次数；
-   步行距离；
-   是否需要复杂跨站操作。

### 9.3 可解释推荐

系统不能只显示：

> 成都东 87 分。

应该显示：

> **推荐成都东站**
>
> -   机场接驳约 65 分钟；
> -   安全时间后仍有较多直达列车；
> -   后续备用车次较多；
> -   相比成都南站，中转容错更高。

------------------------------------------------------------------------

## 10. 目标城市与替代枢纽

### 10.1 默认范围

用户输入"苏州"：

系统首先根据行政区划建立：

`City → Railway Stations[]`

默认仅返回行政意义上属于苏州市的铁路客运站。

### 10.2 附近替代枢纽

用户开启：

`显示附近替代枢纽`

系统增加：

-   周边城市铁路站；
-   周边机场。

### 10.3 V1 替代站规则

第一阶段可采用：

`目标城市行政边界 + 半径阈值`

生成候选集，再进行简单筛选。

不要仅以直线距离作为最终推荐依据。

### 10.4 后续升级

V2 使用：

**替代枢纽 → 用户真实最终目的地的 Door-to-Door 时间**

进行排序。

这将成为 V2 完整 A→B 中转推荐的重要基础。

------------------------------------------------------------------------

## 11. 日期功能

### 11.1 V1：指定日期

必须支持。

用户选择日期后查询该日实际时刻表。

### 11.2 V1.x：灵活日期

输入：

`2026-10-01 ～ 2026-10-04`

系统按日比较：

-   可行车次数；
-   最早安全车次；
-   平均中转余量；
-   总体中转便利程度。

输出"更适合中转的日期"。

### 11.3 日期推荐限制

在没有余票和价格信息时，"推荐日期"只代表：

**交通衔接更便利**

不代表：

-   更便宜；
-   一定有票；
-   客流更少。

UI 必须明确区分。

------------------------------------------------------------------------

## 12. 异常与边界场景

系统必须处理：

### 12.1 同名车站

使用：

`城市 + POI + railway_station_code`

消歧。

### 12.2 跨午夜

例如：

23:10 落地 → 次日 01:00/06:00 列车。

查询必须跨自然日。

### 12.3 公共交通停运

夜间公共交通不可用时：

-   不得继续使用白天地铁时间；
-   自动推荐打车或标记无公共交通方案。

### 12.4 航班延误

V1 不做实时航班状态。

结果提示：

> 当前计算基于计划/用户输入到达时间。

后续可接实时航班 Provider。

### 12.5 铁路数据变化

显示：

`铁路数据更新时间`

数据超过阈值时提示：

> 请在出行前通过官方渠道确认车次。

### 12.6 无直达列车

V1：

默认优先直达。

若无直达，可提示：

> 当前没有发现符合条件的直达列车。

铁路内部二次换乘可作为 V1.x/V2 能力。

------------------------------------------------------------------------

## 13. 技术架构建议

### 13.1 MVP 架构

``` text
Frontend
  │
  ▼
Backend API
  │
  ├── Hub Service
  │     ├── Hub Registry
  │     └── AMap POI Adapter
  │
  ├── Routing Service
  │     └── AMap Direction Adapter
  │
  ├── Railway Service
  │     └── Rail Provider Adapter
  │
  ├── Transfer Engine
  │     ├── Safe Transfer Calculator
  │     ├── Train Filter
  │     └── Hub Ranking
  │
  └── Cache
```

### 13.2 推荐技术栈

个人 MVP：

**Frontend**

-   Next.js / React
-   TypeScript
-   响应式 Web

**Backend**

二选一：

-   Python + FastAPI；
-   TypeScript + Node.js。

若后续路线算法/数据处理较多，推荐 Python + FastAPI。

**Database**

-   PostgreSQL；
-   MVP 极早期可 SQLite。

**Cache**

-   初期内存/数据库缓存；
-   后续 Redis。

### 13.3 API Key 安全

高德 Key 等第三方凭证：

-   不得写入前端源码；
-   通过 Backend Proxy 调用；
-   `.env` 管理；
-   Git 仓库忽略敏感配置。

------------------------------------------------------------------------

## 14. 缓存策略

为降低地图 API 成本：

### Hub

较稳定：

`TTL ≈ 7–30 days`

### 铁路静态时刻

根据 Provider 更新频率设置。

### 公共交通路径

变化较频繁：

`TTL ≈ 数小时～1 day`

### 驾车预计时间

受交通影响明显：

`TTL ≈ 15–60 min`

实际 TTL 应根据高德服务条款、API 配额及产品测试调整。

------------------------------------------------------------------------

## 15. 核心 API 草案

### 15.1 获取城市枢纽

``` http
GET /api/cities/{city}/hubs
```

### 15.2 查询枢纽间接驳

``` http
GET /api/routes
?origin={hub_id}
&destination={hub_id}
&mode=transit
```

### 15.3 查询铁路车次

``` http
GET /api/trains
?origin_city={city}
&destination_city={city}
&date=YYYY-MM-DD
```

### 15.4 中转推荐

``` http
POST /api/transfer/evaluate
```

Request：

``` json
{
  "transfer_city": "成都",
  "arrival_hub": "成都天府国际机场",
  "arrival_datetime": "2026-10-03T14:20:00+08:00",
  "destination_city": "乐山",
  "checked_baggage": true,
  "include_alternative_hubs": false,
  "routing_preference": "balanced"
}
```

Response：

``` json
{
  "recommended_hub": {},
  "candidates": [],
  "safe_transfer_time": {},
  "train_options": [],
  "warnings": []
}
```

------------------------------------------------------------------------

## 16. 性能指标

MVP 目标：

-   首屏可交互：\< 2s；
-   已缓存查询：\< 2s；
-   未缓存完整中转查询：目标 \< 8s；
-   外部 Provider 超时不得导致整个页面永久 Loading；
-   单一 Provider 失败时应尽量返回部分结果。

采用并行查询：

`候选铁路站 A/B/C 的地图路线 + 铁路查询`

应尽可能并行执行。

------------------------------------------------------------------------

## 17. 数据可信度设计

每类数据增加：

``` text
source
updated_at
confidence
```

UI 在必要时显示：

-   实时/近期地图估算；
-   铁路数据更新时间；
-   估算；
-   数据暂不可用。

原则：

**宁可显示"不确定"，不要制造虚假精确度。**

------------------------------------------------------------------------

## 18. 隐私与安全

V1 不需要用户账户。

默认不存：

-   身份信息；
-   真实姓名；
-   订单；
-   支付信息；
-   精确长期位置轨迹。

可匿名记录：

-   查询城市；
-   候选站点击；
-   推荐结果；
-   API 延迟；
-   Provider 错误。

若未来公开运营，应提供隐私政策，并重新审查第三方地图/铁路数据的使用许可。

------------------------------------------------------------------------

## 19. MVP 成功指标

个人阶段不以 DAU 为主要指标。

### 核心产品指标

**Decision Success Rate**

用户查询后是否能快速确定一个中转站。

可通过：

-   点击"选择此方案"；
-   复制车次；
-   打开地图；
-   查询结束行为

近似衡量。

### 其他指标

-   查询成功率；
-   有效候选站覆盖率；
-   平均查询耗时；
-   Provider 错误率；
-   用户修改查询次数；
-   推荐首位候选被采用比例；
-   "无结果"比例。

------------------------------------------------------------------------

## 20. MVP 验收标准

V1 可发布的最低标准：

### AC-01 城市枢纽

输入主要中国大陆城市后，可以正确返回主要机场和铁路客运站。

### AC-02 接驳

任意支持的两个城市内枢纽之间至少可以返回：

-   公共交通时间（可用时）；
-   驾车时间。

### AC-03 铁路

指定日期、起始铁路站、目标城市后可以返回具体铁路车次：

-   车次号；
-   出发时间；
-   到达时间；
-   出发站；
-   到达站；
-   历时。

### AC-04 安全中转

输入飞机到达时间 + 托运状态后，系统能计算：

-   最短可行时间；
-   推荐安全时间；
-   最早推荐铁路车次。

### AC-05 多站排序

存在多个候选铁路站时，系统可以给出排序及推荐理由。

### AC-06 打车比较

结果可同时比较公共交通和打车预计时间。

### AC-07 目标城市多站

输入城市而非具体铁路站时，系统自动覆盖该城市内主要铁路客运站。

### AC-08 替代枢纽

用户主动开启后，可显示目标城市周边替代铁路站/机场，并明确标记为"替代枢纽"。

------------------------------------------------------------------------

## 21. 开发优先级

### P0 --- 必须

-   城市/枢纽标准化；
-   机场/铁路站查询；
-   日期；
-   铁路具体车次；
-   公共交通接驳；
-   驾车/打车时间；
-   托运行李选项；
-   STT 安全中转算法；
-   车次可行性过滤；
-   候选铁路站排序；
-   推荐解释；
-   跨午夜处理。

### P1 --- 强烈建议

-   详细地铁/公交步骤；
-   附近替代铁路站；
-   附近替代机场；
-   灵活日期；
-   缓存；
-   数据更新时间/可信度；
-   分享查询结果链接。

### P2 --- 后续

-   实时航班状态；
-   铁路余票；
-   节假日动态 Buffer；
-   历史晚点模型；
-   铁路二次换乘；
-   个性化中转偏好。

------------------------------------------------------------------------

## 22. V2：A→B 自动中转推荐

V2 将产品问题从：

> "我在 X 中转，X 哪个站最好？"

扩展为：

> "我要从 A 到 B，在哪里中转最好？"

### V2 Route Graph

将交通系统抽象为图：

``` text
Airport
   │ flight
Airport
   │ city transfer
Railway Station
   │ train
Railway Station
   │ city transfer
Destination
```

搜索：

-   飞 → 铁；
-   铁 → 飞；
-   铁 → 铁跨站；
-   飞 → 飞跨机场；
-   周边机场；
-   周边铁路站。

### V2 评分

``` text
RouteScore =
TotalDuration
+ TransferRisk
+ TransferComplexity
+ ScheduleRobustness
```

仍然不需要价格即可形成有价值的路线推荐。

V1 的 Hub Registry、Rail Provider、Routing Provider 和 Transfer Engine
均应设计成 V2 可直接复用。

------------------------------------------------------------------------

## 23. 商业化前置检查

个人使用阶段可以快速验证价值。

公开发布前必须重新审查：

1.  高德 API 服务协议；
2.  API 调用额度及商业使用政策；
3.  铁路时刻数据来源及授权；
4.  是否允许缓存/再展示；
5.  航班数据授权；
6.  地图展示资质及合规要求；
7.  用户隐私政策；
8.  "推荐中转时间"责任免责声明。

**产品架构必须从第一天将数据 Provider 与业务逻辑解耦。**

这样未来即使某个数据源不可继续使用，也可以替换
Provider，而不需要重写整个产品。

------------------------------------------------------------------------

## 24. 关键技术风险

### R1 铁路数据稳定性 --- 高

风险：

公开、稳定、长期可依赖的中国铁路班次数据来源有限。

对策：

-   Provider Adapter；
-   本地缓存；
-   更新时间提示；
-   公开运营前解决授权问题。

### R2 地图 API 成本/配额 --- 中

对策：

-   缓存；
-   候选站预筛选；
-   避免 N×N 无意义路线请求；
-   个人阶段监控实际调用量。

### R3 "安全时间"误导 --- 高

对策：

-   使用区间/等级；
-   参数可解释；
-   明确提示为规划建议；
-   不宣称"保证赶上"。

### R4 POI 错配 --- 中

对策：

-   Hub Registry；
-   alias；
-   POI ID；
-   railway station code；
-   人工纠错机制。

### R5 推荐算法过度复杂 --- 中

对策：

V1 使用透明规则评分，不使用机器学习。

------------------------------------------------------------------------

## 25. 建议开发里程碑

### Milestone 0 --- 技术验证

目标：证明三个关键数据链路可打通。

完成：

1.  输入"成都"返回机场和主要铁路站；
2.  查询天府机场→成都东站公交/驾车时间；
3.  指定日期查询成都东→乐山铁路车次。

三个链路全部成功后再正式开发 UI。

### Milestone 1 --- 单城市 Demo

完成：

`机场 → 多铁路站 → 目标城市`

输出可行车次和推荐站。

### Milestone 2 --- MVP

完成 P0 全部功能，并支持全国主要城市。

### Milestone 3 --- Private Beta

重点验证：

-   推荐是否符合人工判断；
-   STT 是否过松/过紧；
-   铁路数据覆盖；
-   API 调用成本；
-   异常城市/车站。

### Milestone 4 --- Public Beta

只有解决铁路数据授权和第三方 API 商业使用问题后再考虑公开。

------------------------------------------------------------------------

## 26. 第一版开发任务建议

建议不要先写完整 UI。

第一项开发任务应该是一个 CLI / API Proof of Concept：

``` text
Input:
成都
成都天府国际机场
2026-10-03 14:20
乐山
有托运行李

Output:
成都东
  transit: xx min
  driving: xx min
  safe_after: xx:xx
  trains: [...]

成都南
  transit: xx min
  driving: xx min
  safe_after: xx:xx
  trains: [...]

recommendation:
成都东
```

只要这个结果稳定正确，后续 Web UI 的开发风险会显著降低。

------------------------------------------------------------------------

## 27. 最终产品边界总结

### V1

**城市 X 已知**

解决：

> X 有哪些交通枢纽？\
> 我应该去哪个铁路站？\
> 怎么过去？\
> 要多久？\
> 哪些具体车次赶得上？\
> 应该预留多久？\
> 目标城市附近有没有值得考虑的替代站/机场？

### V2

**中转城市未知**

解决：

> 从 A 到 B，系统应该推荐我在哪里、以什么交通组合进行中转？

------------------------------------------------------------------------

## 28. V1 北极星体验

用户不应该为了规划一次飞机→高铁中转：

1.  打开地图逐个搜索机场到不同车站；
2.  再打开铁路 App；
3.  分别搜索多个出发站；
4.  自己计算下飞机、取行李和进站时间；
5.  再在脑中比较所有方案。

理想状态：

> **输入一次 → 看到所有合理候选 → 知道哪些车赶得上 →
> 得到一个可解释的推荐。**

这就是 V1 最核心的产品价值。
