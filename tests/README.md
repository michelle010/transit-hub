# Cross-application tests

Backend unit and API tests live in `apps/api/tests` so they run with the backend's
Python environment. This directory is reserved for future tests that exercise a
complete web-to-API workflow; no end-to-end framework is added in this bootstrap.
